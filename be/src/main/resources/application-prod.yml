spring:
  datasource:
    url: jdbc:mysql://mysql:3306/naos?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: naos1234!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    redis:
      host: redis
      port: 6379

# ==================== Custom (운영) ====================
custom:
  site:
    name: "NAOS"
    cookie:
      secure: false
      sameSite: Lax
    cookieDomain: ""
    frontUrl: "http://3.34.99.116:3000"
    backUrl: "http://3.34.99.116:8090"
  jwt:
    secretKey: "naos-jwt-secret-key-must-be-at-least-32-characters-long-for-security"

# ==================== Logging (운영) ====================
logging:
  level:
    root: WARN
    com.moni.naos: INFO
    org.hibernate.SQL: WARN
