spring:
  datasource:
    url: jdbc:mysql://mysql:3306/naos?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: naos1234!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    redis:
      host: redis
      port: 6379

  # ==================== Mail 설정 (운영 환경) ====================
  mail:
    host: smtp.gmail.com
    port: 587
    username: ilovemamegoma@gmail.com
    password: psiklmgpkuswacij
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# ==================== Custom (운영) ====================
custom:
  site:
    name: "NAOS"
    cookie:
      secure: false
      sameSite: Lax
    cookieDomain: ""
    frontUrl: "https://wearenaos.com"
    backUrl: "https://wearenaos.com"
  jwt:
    secretKey: "naos-jwt-secret-key-must-be-at-least-32-characters-long-for-security"

# ==================== Logging (운영) ====================
logging:
  level:
    root: WARN
    com.moni.naos: INFO
    org.hibernate.SQL: WARN
